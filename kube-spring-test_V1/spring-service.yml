apiVersion: v1
kind: Service

metadata:
  name: spring-service

spec:
  type: NodePort
  selector:
    app: config-test
  ports:
    - protocol: TCP
      nodePort: 30000
      targetPort: 8081
      port: 8080