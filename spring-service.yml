apiVersion: v1
kind: Service

metadata:
  name: spring-service

spec:
  type: LoadBalancer
  selector:
    app: backend-app
  ports:
    - protocol: TCP
      targetPort: 8080
      port: 80 # 외부에서 사용자가 요청하는 포트 번호



